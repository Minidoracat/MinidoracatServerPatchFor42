# Changelog

<!-- 撰寫規則摘要（完整版見 AGENTS.md「CHANGELOG 撰寫規則」）：
  - bullet 寫給玩家，會整段照貼 Workshop 更新說明：症狀先行、遊戲內名詞、
    禁檔名/函式名/行號/引擎術語；影響版本誠實寫清楚
  - 「> 技術要點：」（選用）只放管理員/modder 需要的行為事實；單次變更的完整
    技術原因寫在 commit message 本文（綁 diff、可 git log 搜尋、永不公開）
  - 紅線：不寫攻擊配方（安全修正只寫「強化了驗證」）、不寫玩家名/座標/Steam ID、
    不寫主機名/路徑/IP -->

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [42.20.4-0.3.0] - 2026-09-25

### 效能

- **幻装衣橱（Mirage Wardrobe）讓畫面卡頓**：這個 MOD 每一幀都會重新比對所有在線玩家衣服上的血跡、髒污、破洞與補丁，不管對方離你多遠。在線人數多的時候，這一項就會吃掉大部分運算，管理員帳號特別明顯，因為會收到全服玩家的資料。現在只處理你附近、畫面上看得到的玩家，其他人走進範圍時會立刻補上，塑形外觀的顯示與原本相同。沒有啟用幻装衣橱時，本補丁不做任何事。

> 技術要點：只在客戶端生效，玩家連線時會自動下載，不用另外安裝。實測管理員帳號、伺服器 17 人在線時，這個 MOD 的每幀處理佔了遊戲主執行緒約七成。

## [42.20.4-0.2.0] - 2026-09-06

### 新增

- **車輛改裝材料不足也能裝上的問題**：使用車輛改裝介面（Tsar's Common Library 系列車輛）安裝或拆卸零件時，若動作執行當下背包裡的材料比配方需要的少，原本仍會照樣裝上或拆下零件，只扣掉手上有的那幾件。現在伺服器會先核對每一項材料的件數與用量，任一項不足就不執行這次動作——材料不扣、零件不動，把材料補齊放進背包後再操作即可。材料充足時行為與原本完全相同。沒有啟用該函式庫時，本補丁不做任何事。

> 技術要點：只在伺服器端生效，客戶端不需更新；核對用的查法與該函式庫自己的消耗邏輯一致（同樣的物品名稱規則、可消耗物以剩餘用量計）。被拒絕時伺服器 log 會留一行紀錄（缺哪項、需要多少、實際多少），用來追查背包為什麼會短少。

### 移除

- **台鋸建造補丁**：伺服器已將台鋸 MOD 換成 Machinery（自帶原版建造入口，不需要任何前置），這個補丁失去對象，整個移除。

## [42.20.4-0.1.0] - 2026-09-03

### 新增

- **初始版本**：Minidoracat 伺服器專用的第三方 MOD 客製補丁層。針對本服啟用的 MOD 組合做客製調整，不修改也不重新發布任何他人的 MOD。
- **台鋸可以直接建造了**：使用台鋸 MOD 時，原本必須另外安裝一個建造類前置 MOD 才能把台鋸放到地上，本服沒有裝那個前置，導致台鋸永遠做不出來。現在對著地面按右鍵就會出現「建造台鋸」選項，需要的材料與技能跟原作相同。沒有啟用台鋸 MOD 時，本補丁不會做任何事、也不會多出任何選單項目。

> 技術要點：本補丁對每個上游 MOD 都是軟依賴——執行時偵測，偵測不到就完全不註冊任何東西，因此可以安全地留在伺服器與客戶端的 MOD 清單中。上游 MOD 若自行修好或改變行為，對應補丁需要重新核對；本 repo 有每日的上游更新監看，更新後會另行處理。
